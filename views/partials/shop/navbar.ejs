<nav class="navbar navbar-expand-lg navbar-light fixed-top bg-light shadow">
    <div class="container px-4 px-lg-5">
        <a class="navbar-brand text-primary fw-bolder text-uppercase" href="/"><i class="fa-solid fa-book-quran"></i> Bookadoodle</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-lg-4">
                <li class="nav-item"><a class="nav-link" aria-current="page" href="/">Trang chủ</a></li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" id="navbarDropdown" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">Sản phẩm</a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="/">Tất cả sản phẩm</a></li>
                        <li><hr class="dropdown-divider" /></li>
                         <% if(genres) { %>
                            <% for(let genre of genres ) { %>
                                <li><a class="dropdown-item" href="/the-loai/<%= genre._id%>"><%= genre.genre %></a></li>  
                            <% } %> 
                        <% } %>   
                    </ul>
                </li>
                <li class="nav-item"><a class="nav-link" href="/gioi-thieu">Về chúng tôi</a></li>
            </ul>
            <% if(currentUser) { %>
                <div class="me-2">
                    <span class="text-dark">Hello <%= currentUser.username %></span> | 
                    <a href="/logout" class="text-decoration-none text-dark">Đăng xuất</a>
                </div>
            <% } %> 
            <% if(!currentUser) { %>
                <div class="me-2">
                    <a href="/login" class="text-decoration-none text-dark">Đăng nhập</a> |
                    <a href="/register" class="text-decoration-none text-dark">Đăng ký</a>
                </div>
            <% } %> 
            <div class="d-flex">
                <a class="btn btn-outline-success" href="/gio-hang">
                    <i class="bi-cart-fill me-1"></i>
                    <span class="badge cartBadge bg-success text-white ms-1 rounded-pill">
                        <% if(typeof total !== "undefined") { %>
                            <%= total %> 
                        <% } %> 
                        <% if(typeof total === "undefined") { %>
                        0
                        <% } %> 
                    </span>
                </a>
            </div>
            <div class="mx-1"></div>
            <div class="d-flex">
                <a href="/danh-sach-yeu-thich" class="btn btn-outline-danger">
                    <i class="fa-solid fa-heart me-1"></i>
                    <span class="badge listBadge bg-danger text-white ms-1 rounded-pill">
                        <% if(typeof list !== "undefined") { %>
                            <%= list %> 
                        <% } %> 
                        <% if(typeof list === "undefined") { %>
                        0
                        <% } %> 
                    </span>
                </a>
            </div>
        </div>
    </div>
</nav>